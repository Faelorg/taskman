const loginAPI = (login: string, password: string) => {
  return "something";
};

const restoreAPI = (email: string) => {};

const getCompanyNameAPI = () => {
  return "something";
};

export { loginAPI, restoreAPI, getCompanyNameAPI };
